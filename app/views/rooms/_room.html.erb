<tr>
  <td><%= room.name %></td>
  <td><%= house.name %></td>
  <td><%=link_to 'Details', room_path(room), class: 'btn btn-info btn-xs' %></td>
  <td><%=link_to 'Edit', edit_room_path(room), class: 'btn btn-warning btn-xs' %></td>
  <td><%=link_to 'Delete', room_path(room), method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-danger btn-xs' %></td>
</tr>